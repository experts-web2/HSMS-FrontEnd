import { SubscriptionManagmentDirective } from './subscription-managment.directive';

describe('SubscriptionManagmentDirective', () => {
  it('should create an instance', () => {
    const directive = new SubscriptionManagmentDirective();
    expect(directive).toBeTruthy();
  });
});
