import { SectionVisibilityDirective } from './section-visibility.directive';

describe('SectionVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
